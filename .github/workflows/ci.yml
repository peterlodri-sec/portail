name: CI

# Architecture: This workflow runs on every push to main and every PR.
# Self-hosted runner on dev-cx53 (x86_64-linux) handles Linux jobs.
# macOS jobs use GitHub-hosted runners (no self-hosted macOS available).
# Tests, clippy, and format checks run in parallel across both platforms.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -D warnings
  CARGO_INCREMENTAL: 0

jobs:
  check:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: self-hosted
            label: linux
          - os: macos-latest
            label: macos
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: clippy, rustfmt

      - name: Cache cargo registry
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ matrix.label }}-cargo-registry-${{ hashFiles('Cargo.lock') }}
          restore-keys: ${{ matrix.label }}-cargo-registry-

      - name: Check
        run: cargo check --locked

      - name: Test
        run: cargo test --locked

      - name: Clippy
        run: cargo clippy --locked --all-targets

      - name: Format
        run: cargo fmt --check

  nix:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v7
      - name: Flake check
        run: nix flake check --impure

  python:
    runs-on: self-hosted
    defaults:
      run:
        working-directory: plugins/portail-mcp
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Install
        run: pip install -e ".[dev]"
      - name: Lint
        run: pip install ruff && ruff check
